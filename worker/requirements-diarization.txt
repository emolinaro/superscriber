# diarization-bundle (captain engine ruling 2026-08-20, option A):
# pyannote speaker-diarization-3.1, vendored once per home and run fully
# offline from the local byte cache.
#
# Installed alongside the pinned torch pair - never from this file alone:
# - Host lanes: scripts/install-worker-torch-wheels.sh self-classifies the
#   host, installs torch==2.8.0/torchaudio==2.8.0 from the variant index it
#   picked (or skips the whole stack with a printed notice where no 2.8.0
#   wheel exists, i.e. Intel macOS), then installs this file.
# - Container lanes: the Dockerfile pins the CPU variant of the pair from
#   download.pytorch.org/whl/cpu, then installs this file so no CUDA
#   runtime lands in the image.
#
# Pins explained:
# - pyannote.audio 3.3.2 is the final 3.x release; it supports numpy 2 and
#   current torch (earlier 3.x releases crash on numpy>=2 and python 3.13).
# - torchaudio 2.9 removed set_audio_backend(), which pyannote 3.3.2 still
#   calls at import time, so torch/torchaudio stay on 2.8.0.
# - matplotlib is an undeclared import in pyannote.audio 3.3.mixtures task
#   code (declared only starting 4.x); pinned here explicitly.
pyannote.audio==3.3.2
matplotlib>=3.8
