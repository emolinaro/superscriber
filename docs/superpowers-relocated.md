# docs/superpowers relocated

The internal spec-and-plan tree formerly under `docs/superpowers/` now lives at `.fm-internal/docs-superpowers/` in the local working copy only - it is gitignored and no longer published in the repo; in-repo references (e.g. DESIGN.md) point to that local path, which is absent from fresh clones.
